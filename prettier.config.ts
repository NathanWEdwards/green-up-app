import { type Config } from 'prettier';

const config: Config = {
    singleQuote: true,
    semi: true,
    trailingComma: 'none',
    tabWidth: 4,
    useTabs: false,
    printWidth: 80,
    endOfLine: 'auto'
};

export default config;
